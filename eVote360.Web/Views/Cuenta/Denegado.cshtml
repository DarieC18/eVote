@{
    ViewData["Title"] = "Acceso denegado";
}

<div class="container mt-5">
    <div class="alert alert-warning shadow-sm rounded-4 text-center">
        <h4 class="alert-heading">¡Acceso Denegado!</h4>
        <p>No tienes permisos para acceder a esta sección. Si crees que esto es un error, contacta con el administrador.</p>
        <hr>
        <p class="mb-0">¿Necesitas ayuda? <a href="@Url.Action("Contact", "Home")" class="alert-link">Contacta con nosotros</a>.</p>
    </div>
</div>
