@{
    ViewData["Title"] = "Votación - Identificación";
}

<div class="container py-5">
    <div class="row justify-content-center">
        <div class="col-md-8 col-lg-6">

            <div class="card shadow-lg border-0 rounded-4">
                <div class="card-header bg-primary text-white rounded-top-4">
                    <h4 class="mb-0 fw-semibold d-flex align-items-center">
                        <i class="bi bi-person-badge-fill me-2"></i> Verificación de Identidad
                    </h4>
                </div>

                <div class="card-body p-4">
                    <p class="text-muted mb-4 text-center">
                        Para comenzar el proceso de votación, introduce tu número de cédula.
                        <br /><strong>Tu información será validada de forma segura y privada.</strong>
                    </p>

                    <form asp-action="ValidarIdentidad" method="post">
                        <div class="mb-4">
                            <label for="Cedula" class="form-label fw-semibold">Número de cédula</label>
                            <div class="input-group input-group-lg">
                                <span class="input-group-text bg-light text-muted">
                                    <i class="bi bi-person-vcard"></i>
                                </span>
                                <input type="text" name="Cedula" id="Cedula" class="form-control form-control-lg"
                                       placeholder="Ejemplo: 00112345678" maxlength="11" required />
                            </div>
                            <div class="form-text text-muted small">
                                Asegúrate de escribirlo sin guiones.
                            </div>
                        </div>

                        <button type="submit" class="btn btn-success btn-lg w-100 py-3 shadow-sm">
                            <i class="bi bi-check-circle me-2"></i> Validar identidad
                        </button>
                    </form>
                </div>

                <div class="card-footer bg-light text-center rounded-bottom-4">
                    <p class="small mb-1 text-muted">
                        Este proceso es 100% confidencial. Tus datos solo se usarán para autenticarte.
                    </p>
                </div>
            </div>

            <div class="text-center mt-4">
                <a asp-action="Ayuda" class="btn btn-link text-decoration-none">
                    <i class="bi bi-info-circle"></i> ¿Cómo votar?
                </a>
            </div>

        </div>
    </div>
</div>
