@model string
@{
    ViewData["Title"] = "Mensaje";
}
<div class="alert alert-info mt-3">@Model</div>
<div class="mt-2">
    <a class="btn btn-primary" asp-action="Boleta">Volver a la boleta</a>
    <a class="btn btn-secondary" asp-action="Index">Inicio</a>
</div>
